- name: Deploy to Server via SCP
  uses: appleboy/scp-action@v0.1.4
  with:
    host: ${{ secrets.SERVER_HOST }}
    username: hexo
    port: ${{ secrets.SERVER_PORT }}
    key: ${{ secrets.SERVER_SSH_KEY }}
    source: "public/*"
    target: "/srv/hexo/blog"
